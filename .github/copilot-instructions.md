# Repository Coding Instructions

## Git Branches

When asked to make a branch based on `origin/main`, fetch or otherwise use the current head of `origin/main` as the starting point for a new local branch. Keep the requested branch name and configure it to publish to a same-named branch on `origin` when ready; do not configure it to track or push to `origin/main`.
